var RetentionTable = React.createClass({
    render: function() {
        return (
            <div className="retentionTable">
                Hello, world! I am a CommentBox2.
            </div>
        );
    }

});

React.render(<RetentionTable />, document.body);